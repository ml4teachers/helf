import { redirect } from 'next/navigation';

export default function AuthRootPage() {
  // redirect('/auth/login'); // Temporarily disable redirect for Capacitor build
  return null; // Or render a placeholder if needed
}